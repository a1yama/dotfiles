# Go レビューチェックリスト

## よくあるミス

### 定数定義
- **iota を使った const の途中に新しい定数を追加**
  - 説明: iota で定義された定数の途中に新しい定数を追加すると、それ以降の定数の値が全てずれてしまう
  - 例:
    ```go
    const (
        StatusPending = iota  // 0
        StatusActive          // 1
        StatusNew             // 2 ← 新規追加
        StatusInactive        // 3 ← 元々2だったのが3に
        StatusCompleted       // 4 ← 元々3だったのが4に
    )
    ```
  - 対策: 新しい定数は末尾に追加するか、明示的に値を指定する

- **機能固有ファイルに汎用名のパッケージレベル const を置く**
  - 説明: Go の const/var/func はファイルスコープを持たずパッケージ全体で一意である必要がある。`dateFormat` や `defaultTimeout` のような汎用名を1機能のファイルに定義すると、同じパッケージの別ファイルが同名を定義した時点でコンパイルエラーになり、また既存の同名 const を意図せず使い回して意味の異なる値が適用される事故も起きる
  - 例:
    ```go
    // 1機能のためのファイルに置かれた汎用名の const
    const dateFormat = "2006-01-02" // パッケージ全体を占める。同パッケージの他ファイルと衝突しうる

    const fooDateFormat = "2006-01-02" // 機能接頭辞を付ける
    ```
  - 対策: パッケージ内で1つに絞れないなら機能接頭辞を付けるか、共有パッケージへ切り出す

### エラーハンドリング
- **エラーの無視（`_ = err`）**
  - 説明: エラーを意図的に無視している場合、コメントで理由を明記すべき

- **エラーのラップ不足**
  - 説明: `fmt.Errorf` で `%w` を使わずにエラー情報が失われる

- **共通エラーレスポンスが拾わない型でエラーを返して本文が unexpected_error に潰れる**
  - 説明: 共通のエラーレスポンス生成関数は、たいてい特定のエラー型（独自のコード付きエラー型など）だけを本文に反映し、それ以外は汎用の `unexpected_error` に潰す。ステータスコードだけを付け替えるヘルパーに素の error を渡すと、HTTP ステータスは意図どおりでも本文は汎用エラーになる。さらに共通ハンドラが 500 以外でも APM のエラータグ（`ext.Error` / `ManualKeep` 相当）を立てる実装だと、想定内の 404/422 が予期しないエラーとして強制サンプリングで積まれる
  - 対策: 新しいステータスコードを返す差分を見たら、共通レスポンス生成関数が本文に反映する型でエラーを組み立てているかを確認する。既存の同種エラー定義を1つ開いて形を突き合わせるのが速い
  - 例: 「リソース未生成」の 422 も「他テナントのリソース」の 404 も本文が同じ汎用エラーになり、呼び出し側が区別できない

- **非再試行エラー用の sentinel を定義したのに、リトライ経路の呼び出し側が使っていない**
  - 説明: クライアント層で 404/422 を `ErrXxxNotFound` のような sentinel に変換していても、SQS コンシューマや cron など「エラーを返す＝再試行」の呼び出し側が `errors.Is` で判別していないと、絶対に成功しない処理を最大受信回数まで再実行して DLQ に落とす。せっかく専用エラーへ変換しているのに使われていない、という形になりやすい
  - 対策: sentinel error を追加・利用する差分では、その error が再試行ループの中で返されうるかを確認し、再試行経路では `errors.Is` で早期に打ち切っているかを見る
  - 例: クライアント層が 404 を専用 sentinel に変換しているのに、キュー consumer 側はそれを他のエラーと同列に返すため、絶対に成功しないメッセージが最大受信回数まで再配信される

### テスト

- **テナント分離を SQL の WHERE 句で担保したのに、テストがリポジトリモックだけ**
  - 説明: 「取得後に照合するのではなく検索条件に `tenant_id` を入れる」という設計は正しいが、その保証は WHERE 句1行に集約される。usecase / handler のテストで `FindByXxx` のモックに NotFound を返させても、条件が消えた回帰は検知できない
  - 対策: 検索条件に認可用のカラムを足す差分では、同じディレクトリの `*_integration_test.go`（実 PostgreSQL の suite）に「別テナントの id で NotFound」ケースが追加されているかを確認する。判定は「その条件を差分から削ってテストが落ちるか」
  - 例: 取得メソッドから `AND tenant_id = ?` を削除して全テストを回しても、統合テストを含めて全部緑のままだった

- **インターフェースを埋め込んだ fake が本物の実装と違う値を返し、到達不能な分岐をテストが緑にする**
  - 説明: `type fakeX struct { XService; ... }` のようにインターフェースを埋め込んで一部メソッドだけ差し替える fake は、書き手が想像した返り値（例: 0件で `(nil, nil)`）を返せてしまう。本物の実装が同じ条件でエラーを返す場合、その分岐は本番では到達不能なのにテストは通る
  - 対策: fake を追加する差分では、差し替えたメソッドの**本物の実装**を開き、境界条件（0件・空・nil）での返り値が一致するかを突き合わせる。判定は「本物の実装のその分岐を削ってテストが落ちるか」
  - 例: 呼び出し側の `len(xs) == 0` 分岐は、本物の実装が 0 件時にエラーを返すため実環境では到達しない。fake だけが `(nil, nil)` を返すのでテストは緑になる

### 並行処理
- **goroutine リーク**
  - 説明: context のキャンセルや done チャネルでの適切な終了処理がない

- **defer のループ内使用**
  - 説明: ループ内で defer を使うとリソースがループ終了まで解放されない

### nil 処理
- **nil ポインタチェックの欠如**
  - 説明: ポインタや interface の nil チェックが不足している

### DI・コード生成
- **wire の provider を増減したのに inject.go と wire_gen.go が食い違う**
  - 説明: `cmd/*/inject.go` の provider set から `repository.NewXxx` を削除・追加した際、`wire_gen.go` は手編集ではなく再生成して整合させる。生成物側にだけ古い provider 呼び出しやローカル変数が残るとコンパイルは通るのに未使用の依存が生き続け、逆に inject.go だけ直して wire_gen.go が古いままだと実行時の依存が意図と食い違う
  - 対策: レビュー時は inject.go の差分行と wire_gen.go の差分行が1対1で対応しているかを見る。provider が不要になったら inject.go 側からも消す（wire は未使用 provider をエラーにする）

### 不要コード
- **機能削除時にテストとヘルパーの残骸を消し漏らす**
  - 説明: 本番コードから機能を削除しても、その機能のためだけに書かれたテスト・コメント・ヘルパー関数が残ることがある。残ると「まだこの処理をしている」という誤った仕様理解を招き、未使用のエクスポート関数が生き続ける
  - 対策: 削除 PR では grep で当該ヘルパーの呼び出し元を確認し、テストのみが参照している状態になっていないかを見る

### 可読性・保守性
- **定数を新設したのに同一ファイル内の同じリテラルが残る**
  - 説明: 日付フォーマット等のマジックリテラルを const に切り出す変更で、着手した1〜2箇所だけ置換して同ファイル内の残りがリテラルのまま、という中途半端な抽出が起きやすい
  - 対策: const を新設した差分を見たら、そのファイル（できれば同パッケージ）を同じリテラルで grep して取り残しが無いか確認する。取り残すなら抽出自体を見送る方が一貫する
- **`render.Status` と `render.NoContent` の併記は効かない**
  - 説明: go-chi/render v1.0.1 の `NoContent` は `responder.go:138` で `w.WriteHeader(204)` を直接書き、`render.Status` が request context に積んだステータスを見ない。`render.Status(r, http.StatusNoContent)` を前置しても無意味で、204 以外を渡しても黙って 204 になる。`render.JSON` / `render.Respond` は context のステータスを見るため、同じ感覚で書くと食い違う
  - 対策: `render.NoContent` の直前に `render.Status` が無いかを見る。あれば削除させる（正しい形は `render.NoContent(w, r)` の単独呼び出し）
  - 例:
    ```go
    // NG: render.Status は無視される
    render.Status(r, http.StatusNoContent)
    render.NoContent(w, r)

    // OK
    render.NoContent(w, r)
    ```

### 画像・バイナリ変換
- **透過 PNG をアルファ無しフォーマットへ変換すると透過部が黒に潰れる**
  - 説明: `image/draw` は乗算済みアルファを扱うため、`image.NewRGBA` の初期値 `(0,0,0,0)` に `draw.Over` で合成すると alpha = 0 の領域が `(0,0,0)` になる。これを RGB565 や RGB888 など alpha を持たない形式へ書き出すと、透過背景や白抜き部分が一律で黒になる。コンパイルもテストも通り、実機の表示を見るまで気づけない
  - 対策: 合成先を先に不透明色で塗ってから元画像を重ねる。レビュー時は、アルファ無し形式への変換コードで合成先キャンバスが初期化されているかを確認する
  - 例:
    ```go
    // NG: 初期値が透明（乗算済みで黒）のキャンバスに Over で合成
    dst := image.NewRGBA(image.Rect(0, 0, w, h))
    draw.ApproxBiLinear.Scale(dst, dst.Bounds(), src, src.Bounds(), draw.Over, nil)

    // OK: 背景を不透明色で塗ってから重ねる
    dst := image.NewRGBA(image.Rect(0, 0, w, h))
    draw.Draw(dst, dst.Bounds(), image.NewUniform(color.White), image.Point{}, draw.Src)
    draw.ApproxBiLinear.Scale(dst, dst.Bounds(), src, src.Bounds(), draw.Over, nil)
    ```

### 組込み・TinyGo
- **移動平均や差分計算を符号なし整数で書くと減算のラップで巨大値になる**
  - 説明: `smoothed += (x - smoothed) / 4` のような指数移動平均・差分フィルタを `uint32` / `uint16` で書くと、入力が下降した瞬間に `x - smoothed` が 2^32 近傍にラップし、÷4 して約 1.07e9 という値が残り続ける。コンパイルもテストも通り、センサー値が上昇するケースだけ試すと気づけない
  - 対策: 中間計算を `int32` など符号付きにする。レビュー時は符号なし変数どうしの減算を探し、被減数が減数より小さくなり得るかを確認する
  - 例:
    ```go
    // NG: 値が下降すると (raw - smoothed) がラップする
    var smoothed uint32
    smoothed += (uint32(sensor.Get()) - smoothed) / 4 // 40000 -> 1000 で 1073772074

    // OK: 符号付きで計算する
    var smoothed int32
    smoothed += (int32(sensor.Get()) - smoothed) / 4 // 30250
    ```
- **ドライバが設定済みのピンを PWM や別機能として奪うとき、ドライバ側の参照箇所を全数確認していない**
  - 説明: `st7789` のように `Configure()` でバックライトピンを Output + High にするドライバの場合、その後 `machine.PWMx.Channel(pin)` で FUNCSEL を奪うのは動く。しかしドライバの他メソッド（`EnableBacklight` 等）が後から `pin.High()` / `pin.Low()` を呼ぶと PWM 設定が壊れる
  - 対策: ドライバのソースを当該ピンのフィールド名で grep し、参照が初期化系メソッドだけに閉じているかを確認する。呼んではいけないメソッドはコード側のコメントに明示する
- **RP2040 の PWM はスライス単位で周期を共有するのにピン割り当てで衝突を確認していない**
  - 説明: RP2040 の PWM スライス番号は `(gpio >> 1) & 7`、チャネルは `gpio & 1` で決まる。GPIO14 と GPIO15 のような隣接ピンは同一スライスになり TOP / DIV を共有するため、片方を 1kHz の調光、もう片方を 38kHz の赤外線搬送波のように使うことはできない
  - 対策: 新たに PWM 化したピンについてスライス番号を計算し、同一スライスの相方ピンが他の用途で PWM を使っていないかを確認する
### 数値・型変換
- **表示・帳票への詰め替えで float64 を int64 に素キャストして検算が崩れる**
  - 説明: DB が float64（numeric）で持つ単価・数量を、PDF や CSV など「受け取った側が数量×単価で検算する」出力に `int64(v)` で詰め替えると、小数分が黙って切り捨てられ、同時に出力する合計金額（DB 側で decimal 計算済み）と辻褄が合わなくなる。丸め誤差対策として `math.Round` を使っている箇所が同じファイルにあるのに、別の箇所だけ素キャスト、という不揃いになりやすい
  - 対策: 差分内の float から `int64(` / `int(` への変換を洗い出し、(1) 元の値が小数を取りうるか、(2) 出力先で他の値との整合が要求されるか を確認する。整数前提なら黙って切り捨てず、明示的にバリデーションしてエラーにする
  - 例: 単価 27.5 × 数量 10 で金額 275 なのに、帳票には「数量 10 / 単価 27 / 金額 275」と出て受け取った側の検算が合わない

### データ整合性
- **過去の証憑を再生成する処理でマスタの一部だけ Unscoped にして片手落ちになる**
  - 説明: 発行済みの請求書・帳票を後から組み立て直す処理では、参照するマスタが論理削除・名称変更されている可能性がある。片方（消費税マスタなど）だけ Unscoped 取得を用意し、他方（パートナー名・サービス名など）は通常スコープのままにすると、論理削除された瞬間に再生成が恒久的に失敗したり、発行時と異なる内容の証憑ができたりする
  - 対策: 「発行時点の値が要る」と説明されている参照が差分にあれば、同じ再生成パスで引く他のマスタ参照もすべて同じ扱い（スナップショット保存 or Unscoped）になっているかを確認する
  - 例: 税率マスタは Unscoped で引くのに宛名マスタは通常スコープのままで、取引先を論理削除した瞬間に帳票の再生成が NotFound で恒久失敗する
- **`<column> IS NULL` スキャンで再試行する設計に打ち切りが無いと、恒久失敗が毎回の実行で再発し続ける**
  - 説明: 「完了カラムが NULL のものを毎回拾い直す」方式は、失われた処理を次回実行で回収できる反面、回収不能な失敗（参照先マスタの論理削除、存在しない宛先など）に終端が無い。該当レコードは永久に NULL のまま残り、毎回の実行で同じ失敗が Slack 通知やログに積まれ、運用側から消す手段が無くなる。さらに外部への送信をともなう工程では、再実行のたびに相手にメール等が届き得る
  - 対策: 未完了スキャンを追加する差分では (1) 件数上限や対象期間の絞り込みがあるか、(2) 再試行回数・打ち切り条件・手動で完了扱いにする手段があるか、(3) 参照するマスタが論理削除されうるか を確認する
  - 例: `notified_at IS NULL` のレコードを全件拾って通知を出すため、初回デプロイ時に過去分へ一斉送信され、参照先が論理削除された 1 件は毎回失敗し続ける
- **構成的採番の一意制約が、実際の発行単位より粗い粒度の ID で組まれている**
  - 説明: 採番テーブルを避けて `{prefix}-{yyyyMM}-{親ID}` のように親エンティティの ID から番号を組み立てつつ、レコード自体は子エンティティ単位で作る設計は、親が子を複数持った瞬間に UNIQUE 違反で 2 件目以降が恒久失敗する。「再実行しても同じ番号が再現される」冪等性を狙った設計ほど採番の入力を絞りがちで起きやすい
  - 対策: 採番関数の引数に使う ID と、テーブルの UNIQUE 制約に含まれる ID が一致しているかを確認する。一致しないなら、本番データで親:子が 1:1 に収まっている根拠をテストかコメントで固定する
  - 例: 採番は親 ID 基準なのに、テーブルは採番列の UNIQUE と (子 ID, 対象月) の UNIQUE を両方持つ。親が子を 2 つ持つと 2 件目が毎月落ちる
- **2 フェーズ処理で後段だけ未完了スキャンによる再開手段を持ち、前段の失敗が回収されない**
  - 説明: 「フェーズ1でレコードを作る → フェーズ2で完了カラムが NULL のものを引き直す」構成では、フェーズ2の失敗は次回実行で回収される一方、フェーズ1の失敗は「レコードが存在しない」という不在でしか表現されないため回収経路が無い。定時実行で対象期間が毎回変わるジョブでは、失敗した回の対象は二度と拾われない
  - 対策: 未完了スキャンによる再開設計を見たら、その前段にも同等の再開手段（対象の引き直し、または非ゼロ終了による検知）があるかを確認する
  - 例: 後段は完了カラムが NULL のものを拾い直せるが、前段の失敗は ERROR ログのみで exit 0。翌月の実行は対象期間が変わるため拾わない
- **新テーブルを参照する定期ジョブを足すとき、論理削除ジョブ側が新テーブルを削除対象に含めているか確認していない**
  - 説明: 取引先の解約処理のような一括論理削除ジョブは、対象テーブルをハードコードで列挙している。後から追加した設定・契約系テーブルが列挙漏れすると、親が論理削除された後も設定行だけが有効なまま残り、その行を起点に走る定期ジョブが毎回 NotFound で恒久失敗する
  - 対策: 「有効な設定を列挙して処理する」ジョブを追加する差分では、その設定テーブルが既存の論理削除ジョブの列挙に含まれているかを grep で確認する
  - 例: 解約ジョブが新しい設定テーブルを落とさないため、解約済みの対象が列挙クエリに返り続け、本体レコードの取得が NotFound になる

### 外部ライブラリの状態を写す
- **ライブラリの内部状態をコールバックで写すとき、写す側の集合がライブラリの判定より狭い**
  - 説明: ライブラリが状態を公開していないため、変更通知のコールバックで自前の構造体に写し取り、それを見て処理の行き先を決め、結果をさらにライブラリへ渡すことがある。このとき、ライブラリが判定に使う値（列挙定数など）の一部を写し漏らすと両者の判断が食い違う。ライブラリは有効とみなして処理する状態なのに、自前の分岐は無効とみなして捨てたり別の処理へ回したりする
  - 対策: 写し取る構造体を足すときは、ライブラリがその状態を読んでいる箇所のソースを読み、判定に使っている値をすべて拾っているか突き合わせる。拾う値の一覧をテストで固定する
  - 例: 端末エミュレータのマウス追跡モードを写すとき、ライブラリは 5 種類のどれかが立っていれば追跡中とみなすのに、自前の構造体は 4 種類しか拾わない。漏れたモードだけを要求するアプリでは、クリックが捨てられホイールが別の操作に化ける
- **受けたイベントと送り出す先で符号化形式が違うのに、イベントをそのまま中継する**
  - 説明: 受信側の形式では意味を持つ値が、送信側の形式では別の意味になることがある。端末のマウスでは、SGR 形式の離しには離したボタンの番号が付くが、X10 形式には離しの印が無く「ボタンなし」が離しを表す。SGR で受けた離しをそのまま X10 で符号化させると押下として届き、受け手はボタンが押されたままだと判断する
  - 対策: 中継するイベントは、送り出す側の形式で意味が保たれるかを形式ごとに確かめ、必要なら送る前に形式に合わせて直す。出力のバイト列を形式ごとにテストで固定する

## 参考資料
- [Effective Go](https://go.dev/doc/effective_go)
- [Go Code Review Comments](https://github.com/golang/go/wiki/CodeReviewComments)
