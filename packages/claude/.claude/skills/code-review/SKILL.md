---
name: code-review
description: コード変更のレビューを独立したエージェントで実行し、別モデル(Codex)のクロスレビューと突き合わせる。コードの実装・修正が完了した後に自動的に使用する。「レビューして」「コードレビュー」に加えて、「バグが入っていないか見て」「この変更おかしくないか」「境界値まわりが不安」「見落としが無いか確認して」のようにレビューという語を使わない依頼でも必ず使う。自分で git diff を読んで指摘すれば済むと感じても、先にこのスキルを読むこと（別モデルのクロスレビューはここでしか起動しない）。差分をブラウザで目視したいだけなら diff-review、作業方針そのものの点検なら frame-check を使う。
context: fork
agent: Explore
---

## レビュー対象

以下のコマンドで変更差分を取得し、レビューしてください。

```bash
git diff
git diff --cached
```

未追跡の新規ファイルがある場合は、ファイル内容を直接読んでレビューしてください。

```bash
git status --short
```

## 別モデル(Codex)によるクロスレビュー

自分と同じモデルは同じ判断基準で読むため、自分のレビューだけでは見落としが残る。**最初に** Codex のレビューを起動し、その結果と自分のレビューを突き合わせて報告する。

```bash
codex exec review --uncommitted
```

- Bash の `timeout` は `600000` を指定する(数分かかる)
- 差分が Git 管理下にない、`codex` が未インストール、認証切れ、タイムアウトなどで失敗した場合は**中断せず**自分のレビューだけ続行し、報告の冒頭に「Codex レビュー: 実行できず(理由)」と明記する
- Codex の指摘を無批判に採用しない。差分を自分で読み、次のいずれかに分類する
  - **採用** — 実際の不具合。指摘に含める
  - **誤検知** — 差分やコードベースの事実と合わない。報告の「Codex 指摘の棄却」節に理由付きで1行記載
  - **nit** — 妥当だが軽微。重要度 low として含める

## レビュー観点

1. **バグ・ロジックエラー** — 意図しない動作、エッジケースの見落とし、off-by-one エラー
2. **セキュリティ** — インジェクション、ハードコードされた秘密情報、安全でない入力処理
3. **可読性・保守性** — 不明瞭な命名、不必要な複雑さ、過剰な抽象化
4. **一貫性** — 既存コードベースのスタイル・パターンとの整合性

## 言語固有のチェックポイント

変更されたファイルの言語に応じて、`languages/` ディレクトリ内の該当ファイルを参照してください:
- Go → `languages/go.md`
- TypeScript/JavaScript → `languages/typescript.md`
- PHP → `languages/php.md`
- Python → `languages/python.md`
- Rust → `languages/rust.md`
- Shell/Bash(フック・スクリプト) → `languages/shell.md`

各ファイルには、言語特有のよくあるミスと参考資料がまとめられています。

## 出力フォーマット

問題がある場合のみ報告してください。問題がなければ「問題なし」と報告してください。

各指摘は以下の形式で簡潔に:

```
[重要度: high/medium/low][出典: claude/codex/両方] ファイルパス:行番号
内容: 問題の説明
提案: 修正案
```

Codex の指摘を棄却した場合は、末尾に以下の節を付ける:

```
### Codex 指摘の棄却
- <指摘の要約> — 棄却理由: <差分・コードベースに照らした事実>
```

## レビューループ

報告の末尾に、必ず次のどちらかを明記してください。

- 指摘あり: 「**呼び出し元へ: 指摘を修正した後、code-review を再実行してください。`問題なし` になるまで繰り返します（最大3回。超えたら残課題を列挙してユーザーに判断を仰ぐ）。**」
- 指摘なし: 「問題なし」

## チェックリスト追加候補

レビュー中に `languages/<言語>.md` に未掲載の「再発しそうなミスのパターン」を見つけた場合は、該当言語のチェックリストを読んで重複がないことを確認した上で、報告の末尾に以下の節を含めてください。

```
### チェックリスト追加候補
- 言語: <言語> / カテゴリ: <カテゴリ> / 概要: <一行> / 説明: <詳細>
```

**呼び出し元へ: この節が報告に含まれていた場合は、update-review-checklist スキルを候補の内容で実行し、チェックリストに反映してください（ユーザーへの確認は不要）。**
